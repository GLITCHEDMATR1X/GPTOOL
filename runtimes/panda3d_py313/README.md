# panda3d_py313 sidecar runtime placeholder

Place a reusable Python 3.13 runtime with Panda3D here when you want the GPT Tool to run Panda3D smoke/screenshot tests without relying on the system Python.

This package intentionally does not include Python, Panda3D, fonts, or third-party binaries.
