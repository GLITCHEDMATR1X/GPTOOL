# HoloVerse GPTOOL Patch Gate Approved Patch

Extract this zip over the folder that contains `HoloVerse/`.

Then run:

```text
HoloVerse\APPLY_PATCH_GATE_CLEANUP.bat
```

This patch applies the approved HoloCore pass chain plus Vector Arena Pass 98. The cleanup script removes stale HoloUtopia / Etch-Line folders only after the route validators confirmed they are not active routes in this build.
